#!/usr/bin/env ruby

$LOAD_PATH << File.dirname(__FILE__) + '/lib'
require 'fizz_buzzer'

ui = FizzBuzzer::CLI.new($stdin, $stdout)
modes = FizzBuzzer::ModeContainer.setup

app = FizzBuzzer::App.new(ui, modes)
app.start!
